# MeeTap 1.5.0 — 开箱即录，无感采集

macOS 会议录制 + AWS 转录 + LLM 纪要工具。这一版最大的变化：**在 macOS 14.4+ 上录会议，不用再装虚拟声卡、不用切换扬声器、不用担心录着录着卡死丢录音**。纪要生成、终端界面、邮件排版也都做了实打实的改进。

> 注：macOS 14.4+ 指 Sonoma 14.4 及之后的所有版本，涵盖 macOS 15 Sequoia、macOS 26 Tahoe（截至 2026 年 7 月最新为 26.6）。

一句话看懂：**打开会议 → `meetap start` → 开完 `meetap stop`**，剩下的转录、纪要、发邮件全自动，账户里不留任何常驻资源。



## 🎧 一、录音更省心：免装虚拟声卡，开箱即录

**价值**：旧方案依赖 BlackHole 虚拟声卡，需要安装声卡（`brew install blackhole-2ch`）、把系统输出切到 BlackHole、并在各会议 App 里逐一配置扬声器，任一环节出错就会录到静音。1.5.0 在 macOS 14.4+ 上改用系统原生的 Process Tap，这些前置配置全部不再需要。

- **免装、免配置、免切设备**：macOS 14.4+ 直接用系统原生的 Process Tap 采集，安装后即可录制，播放照常从原扬声器 / 耳机输出，录制期间无需切换系统输出设备。
- **消除录制中途卡死、录音丢失**：麦克风与系统音在同一进程内混音、由单个 ffmpeg 负责编码，从根本上消除设备抢占；连续录制多场也不会因上一场的残留进程占用音频链路而无法启动。
- **旧系统兼容**：macOS 13.x 自动回退到 BlackHole 方案，功能不变。

  > **Process Tap 是什么、为什么更好**：它是 macOS 14.4 引入的系统级能力，相当于在系统混音总线上"接一根监听探针"——在音频送往扬声器的同一条链路上旁路复制一份交给录制程序，原声照常播放、听感零变化。相比旧的 BlackHole 虚拟声卡：无需安装任何软件、不改变默认输出设备、不抢占音频设备，且直接采用音频的真实采样率，不会出现时长错拉、音调失真的问题。

- **首次授权有引导**：`meetap setup` 会自动检测系统音频录制权限并引导完成授权。



## 🎨 二、录制时实时波形：系统音 / 麦克风 双路可视

**价值**：录制过程中终端底部实时显示电平波形，同时反映系统音与麦克风，可随时确认采集正常、音频有输入，无需盲等到会议结束才发现异常。

- **点阵波形电平条**：底部一条 30 格滚动的点阵波形（`⠀⡀⡄⡆⡇⣇⣷⣿`），随讲话的音节与停顿自然起伏。

- **系统音与麦克风双路合成**：波形融合会议对端的声音与本机麦克风——任一方有人讲话即会跳动，长时间无起伏即提示采集异常；轻微环境噪声不会干扰。

- **终端界面美化（可选）**：安装 `gum` 后自动启用更美观的终端界面，未安装则降级为纯文本，非强制依赖。

  

## 📧 三、纪要邮件更专业：HTML 排版

**价值**：如果你配了自动发邮件，收件人拿到的是排版干净、在 Outlook 里也不乱的 HTML 纪要，而不是一坨纯文本。

- **纯 HTML 邮件版本**：表格、标题、列表都正确渲染，兼容 Outlook。
- **全宽通知式排版**：借鉴 AWS 内部通知风格，去掉冗余的重复标题和多余留白，读起来更紧凑。
- **「行动计划 & 待确定事项」看得更清**：用表格带边框。



## 📝 四、纪要质量更高、更好用

**价值**：纪要更贴近真实的会议记录、目录名/文件名一看就知道是哪场会，未来可供Agent批量处理；还能补充相关背景资料、提升纪要的准确性。

- **升级到 Claude Sonnet 5**：默认用更强的模型生成纪要。
- **纪要文件自动智能命名**：用会议主题命名成 `日期_时间_主题.md`，一堆会议里也能一眼找到。
- **中途失败不用担心，可通过`meetap again` 断点续跑**：上传、转录或纪要生成任何一步失败了，直接 `meetap again` **从断点继续，**直到完成。
  - 已转录成功、仅纪要未生成：只重跑纪要生成这一步；
  - 转录也未成功：从上传 + 转录开始把整条链路重跑一遍；
  - 旧纪要自动归档到 `archive/`（加时间戳），不会覆盖丢失。
  - 默认处理最近一场，也可指定目录：`meetap again 20260729_0900_季度评审`。
- **补充素材 / 重新生成**：可先把参考资料放进会议目录的 `materials/`（议程 / PPT 导出 / 背景文档，支持 md/txt/pdf/docx/html，帮模型把人名、术语写对），或带上补充要求：`meetap again '' '请用要点列表重写，突出决策项与负责人'`。
- **纪要更"像人写的"**：优化了提示词，控制篇幅、去重、去掉 AI 腔，回归会议纪要该有的样子。
- **开始录制可命名会议**：`meetap start 季度评审`；录音默认保留在本地，方便回看。



## ⏱️ 五、指定时长，到时自动结束录制

**价值**：预先设定会议时长，到达时限后自动结束录制并转录，无需守在终端前手动停止。

- **`meetap start -d 60`**：指定会议时长上限（分钟），到达时限自动结束并转录；启动时即显示**预计结束时刻**（如"60 分钟后（约 15:30）"）。
- **取消静默自停**：移除了旧的"静默 N 秒自动停止"机制——会议中的正常静默，不应终止录制。



## 🐛 六、录音质量与稳定性修复

**价值**：录制音频的时长准确、音调正常、无杂音爆音，安装后即可正常录制。

- **修复时长错拉、音调失真**：采集与编码统一采用音频的真实采样率。
- **修复偶发爆音杂音**：优化实时音频线程，消除录制过程中的音频毛刺。
- **修复安装后无法录制**：修正安装环节的签名与权限问题（此前新安装的用户首次录制会被系统拦截）。



## 🔐 七、更安全、成本可控

**价值**：所有音频和纪要只在你自己的 AWS 账号里流转，权限收到最小，跑一场会大约几毛钱。

- **最小权限 IAM 策略**：按官方权限参考逐条收紧，S3 桶锁死公共访问，附录里给了可直接套用的策略。
- **数据主权**：不经任何第三方云服务，任务结束自动清理临时资源。



## 📦 升级须知

> ### ⚠️ 升级到MeeTap v1.5.0的用户请先看这里
>
> 升级前，确认您的**macOS 版本 在14.4+以上**，截至 2026 年 7 月最新为 macOS 26.6。
>
> MeeTap v1.5.0 默认改用 **Process Tap** 采集，它需要一项你之前从未授予的权限——**"系统音频录制"**（旧版走 BlackHole，不涉及此权限）。若不提前授权，升级后第一次录制可能录到全静音的系统声、开完会才发现。
>
> **升级后先跑一次 `meetap setup` 确认授权，再开始正式会议最稳妥。** `setup` 会自动做一次 2 秒实测：
>
> - **探测为静音时** → `setup` 会自动打开 **系统设置 › 隐私与安全性 › 屏幕与系统录音**，把你的终端 App（Ghostty / Terminal / iTerm 等）加进"仅系统录音"，按提示复测；
> - **务必在探测通过后再开始正式会议**：`auto` 模式在 macOS 14.4+ 上会直接走 Process Tap，若授权未生效，录制不会自动回退、会录到全静音。授权前如需应急录制，可先在配置里把 `audio_capture` 改为 `blackhole` 走旧方式（需已安装 BlackHole）。

- **macOS 14.4+**：`auto` 模式自动走 Process Tap；**升级后先跑一次 `meetap setup` 完成"系统音频录制"授权**（见上方须知）。

- **macOS 13.x**：仍走 BlackHole，需 `brew install blackhole-2ch`。

- **新增配置项**：`audio_capture`（`auto` / `tap` / `blackhole`，一般保持 `auto`）、`visualizer`（波形样式）；旧的静默自停配置（`silence_*`）已移除。

- **默认模型**：`global.anthropic.claude-sonnet-5`。

- **可选依赖**：`gum`（终端界面美化，不装也能用）。

  

## ✅ 验证状态（如实标注）

| 范围 | 状态 |
|---|---|
| Process Tap 采集 / 混音 / 采样率修复 / 到点自停 / again 重生成 / 终端波形 | **已完整实测**（本机 macOS 14.4+ 日常路径） |
| 实时线程优化 | 已实测无回归（真实录音，时长 / 采样率 / 音量均正常） |
| BlackHole 回退路径 | 保留但本轮**未实测**（本机走 Process Tap） |
| 麦克风与系统采样率不一致的告警 | 逻辑已实现、编译通过，但本机两者相同，**未能自然触发验证** |
