# MeeTap - 用户指南

通过 macOS 内部音频（BlackHole）自动录制 Teams / Zoom / 飞书 / 腾讯会议，无需屏幕录制权限。

---

## 快速开始

```bash
# 开会前
meetap start

# 开完后
meetap stop
```

就这么简单。录音文件保存在 `~/Record/MMDD_HHMM/` 目录下，录制结束后自动转录为文字。

---

## 首次使用

### 前提条件

以下软件需要已安装（正常情况下已装好）：

| 软件 | 用途 | 安装方式 |
|------|------|---------|
| BlackHole 2ch | 虚拟音频通道 | `brew install blackhole-2ch` |
| ffmpeg | 音频录制编码 | `brew install ffmpeg` |
| AWS CLI | 自动转录（AWS Transcribe） | `brew install awscli` |

### 一键配置会议 App

```bash
meetap setup
```

该命令会依次配置：
1. **Zoom** — 自动将扬声器改为 "Meeting Multi-Output"（全自动，无需操作）
2. **Teams** — 打开设备设置页面，手动选择 "Meeting Multi-Output (Aggregate)"
3. **腾讯会议** — 打开应用，引导手动在音频设置中选择 "Meeting Multi-Output"

> 此配置只需执行一次。也可以单独配置：
> - `meetap setup-zoom`
> - `meetap setup-teams`
> - `meetap setup-wemeet`

### 飞书

飞书默认跟随系统输出，无需额外配置。

---

## 日常使用

### 开始录制

```bash
meetap start
```

脚本会自动完成以下操作：
1. 检测你当前使用的音频输出设备
2. 创建「当前设备 + BlackHole」多输出设备
3. 切换系统音频输出
4. 开始录制

输出示例：
```
📍 当前音频输出: MacBook Pro Speakers
🔧 切换系统输出到 BlackHole 2ch ...
✅ 系统输出已切换到: BlackHole 2ch
🔧 启动音频转发: BlackHole → MacBook Pro Speakers ...
✅ 音频转发已启动 → MacBook Pro Speakers

🎙️  录制已开始
   文件: ~/Record/0417_1400/meeting_20260417_140000.m4a
   播放: MacBook Pro Speakers

   停止录制: meetap stop
```

### 停止录制

```bash
meetap stop
```

输出示例：
```
⏳ 等待录制完成...
🔊 音频输出已恢复: MacBook Pro Speakers

✅ 录制已保存
   文件: ~/Record/0417_1400/meeting_20260417_140000.m4a
   大小: 26.5 MB
   时长: 28分13秒

📝 后台转录已启动（完成后会收到通知）
```

转录在后台自动进行，不阻塞终端。完成后：
- 会话目录下生成 `transcript.txt`（带说话人标签）
- macOS 弹出通知提醒

```
~/Record/0417_1400/
├── meeting_20260417_140000.m4a   # 录音文件
├── transcript.txt                # 转录文本（自动生成）
├── transcribe-raw.json           # Transcribe 原始结果
└── transcribe.log                # 转录过程日志
```

### 查看状态

```bash
meetap status
```

录制中的输出示例：
```
🎙️  录制中 (PID: 12345)
   文件: ~/Record/0417_1400/meeting_20260417_140000.m4a
   播放设备: MacBook Pro Speakers
   音频转发: 运行中
```

未录制时的输出示例：
```
⏹️  未在录制
```

### 配置会议 App

```bash
meetap setup
```

输出示例：
```
=== 配置 Zoom ===
✅ Zoom 扬声器已设置为: Same as System

=== 配置 Teams ===
🔧 正在创建 Meeting Multi-Output 设备...
✅ 多输出设备已创建 (ID: 185)
📎 正在打开 Teams 设备设置页...
👉 请在 Teams 中手动选择 Speaker → Meeting Multi-Output (Aggregate)

=== 配置腾讯会议 ===
📎 正在打开腾讯会议...
👉 请进入: 设置 → 音频 → 扬声器 → 选择 Meeting Multi-Output
```

---

## 录音与转录文件

每次录制会在 `~/Record/` 下自动创建一个以日期时间命名的文件夹（如 `0417_1400`），所有相关文件集中存放：

```
~/Record/0417_1400/
├── meeting_20260417_140000.m4a   # 录音文件（AAC 128kbps 单声道，约 1MB/分钟）
├── transcript.txt                # 转录文本（带说话人标签，stop 后自动生成）
├── transcribe-raw.json           # AWS Transcribe 原始 JSON 结果
└── transcribe.log                # 转录过程日志
```

如果录制停止后未产生录音文件，空文件夹会被自动删除。

---

## 录制内容说明

| 内容 | 是否录入 |
|------|---------|
| 对方说话的声音 | 是（通过系统音频捕获） |
| 你自己说话的声音 | 是（通过麦克风捕获） |
| 系统通知声、其他应用声音 | 是（录制期间所有系统音频都会被捕获） |

> 建议戴耳机开会录制，避免麦克风拾取扬声器回声。

---

## 切换音频设备的情况

不需要任何额外操作。每次执行 `start` 时，脚本会自动检测当前系统输出设备并重建多输出设备。

例如：
- 在工位外放开会 → `start` 自动用 MacBook 扬声器
- 用有线耳机开会 → `start` 自动用有线耳机
- 用 AirPods 开会 → `start` 自动用 AirPods

---

## 常见问题

### Q: 录制后听不到声音？

检查会议 App 的扬声器设置是否为 "BlackHole 2ch"：
   - Teams: 设置 → Devices → Speaker
   - Zoom: 设置 → Audio → Speaker（或 "Same as System"）
   - 腾讯会议: 设置 → 音频 → 扬声器

如果不是，重新执行 `meetap setup`。

### Q: stop 后其他应用没声音？

脚本 stop 时会自动恢复音频输出。如果异常退出（如终端被关闭），手动恢复：

```bash
# 查看可用输出设备
~/bin/audio-multi-output list

# 手动切回，例如切到 MacBook 扬声器
~/bin/audio-multi-output set-default <设备ID>
```

或者直接在系统设置 → 声音 → 输出中选择你的耳机/扬声器。

### Q: 录音文件是空的或很小？

检查 ffmpeg 日志：
```bash
cat /tmp/meeting-record/ffmpeg.log
```

常见原因：BlackHole 未正确安装，或会议 App 的音频没有走系统输出。

### Q: 同时开了两个会议 App，能录到吗？

能。所有通过系统输出的音频都会被 BlackHole 捕获并混录到同一个文件中。

### Q: 转录没有生成 transcript.txt？

检查转录日志：
```bash
cat ~/Record/0417_1400/transcribe.log
```

常见原因：
- AWS CLI 未安装或未配置凭证（`aws sts get-caller-identity` 验证）
- 网络问题导致 S3 上传失败
- 录音文件过短或无有效音频

转录失败不影响录音文件保存，可稍后手动重试。

### Q: 重启电脑后需要重新配置吗？

不需要。Teams、Zoom、腾讯会议的扬声器设置都是持久化的。`start` 时会自动重建多输出设备。

---

## 命令速查

| 命令 | 说明 |
|------|------|
| `meetap start` | 开始录制 |
| `meetap stop` | 停止录制 |
| `meetap status` | 查看录制状态 |
| `meetap setup` | 一键配置 Zoom + Teams + 腾讯会议 |
| `meetap setup-zoom` | 仅配置 Zoom |
| `meetap setup-teams` | 仅配置 Teams |
| `meetap setup-wemeet` | 仅配置腾讯会议 |
